# Legacy root entry

The root `index.html` and files grouped under `legacy/` are a frozen compatibility surface for the current branch-based GitHub Pages deployment.

Active development lives in `apps/web`. Do not add new behavior under `legacy/`. After GitHub Pages is switched to the Actions deployment, the root entry and this compatibility directory can be removed in one commit.
